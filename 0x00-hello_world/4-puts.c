#include <stdio.h>
/**
 * main - we are writing a command that prints a setence on a new line without using the printf command
 *
 * return: Always 0 (success)
 */

int main() 
	{
		puts("Programming is like building a multilingual puzzle");
		return (0);
	}
